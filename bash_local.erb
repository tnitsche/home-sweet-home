# ~/.bash_local
# e.g. export RAILS_ENV=staging

# Uncomment for rvm
#[[ -s $HOME/.rvm/scripts/rvm ]] && source $HOME/.rvm/scripts/rvm

# Uncomment for rbenv
#export PATH="$HOME/.rbenv/bin:$PATH"
#eval "$(rbenv init -)"
