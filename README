Installation:

  git clone git://github.com/tnitsche/home-sweet-home.git ~/.home-sweet-home
  cd ~/.home-sweet-home
  rake install

If there are errors while pushing, check .git/config. Remote origin has to be

  [remote "origin"]
  fetch = +refs/heads/*:refs/remotes/origin/*
  url = git@github.com:tnitsche/home-sweet-home.git
